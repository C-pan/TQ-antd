import { change_path} from './actionType'

export const change_path_fun = (val)=>({
        'type': change_path,
        'value': val
    })
export const click_path_fun = (val)=>({
        'type': change_path,
        'value': val
    })
